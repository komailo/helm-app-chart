{{- $backupTracker := dict "enabled" false }}
{{- $defaults := default (dict) .Values.defaults }}
{{- $defaultBackup := default (dict) $defaults.backup }}
{{- $defaultPruning := default (dict) $defaultBackup.pruningPolicy }}
{{- $defaultSchedule := default "@hourly" $defaultBackup.schedule }}
{{- $defaultHousekeepingSchedule := default "@daily" $defaultBackup.housekeepingSchedule }}
{{- range $pvcName, $pvc := .Values.persistentVolumeClaims }}
{{- if and $pvc.backup $pvc.backup.enabled }}
{{- $pruning := default (dict) $pvc.backup.pruningPolicy }}
{{- $keepLast := default $defaultPruning.keepLast $pruning.keepLast }}
{{- $keepHourly := default $defaultPruning.keepHourly $pruning.keepHourly }}
{{- $keepDaily := default $defaultPruning.keepDaily $pruning.keepDaily }}
{{- $keepWeekly := default $defaultPruning.keepWeekly $pruning.keepWeekly }}
{{- $keepMonthly := default $defaultPruning.keepMonthly $pruning.keepMonthly }}
{{- $keepYearly := default $defaultPruning.keepYearly $pruning.keepYearly }}
{{- $housekeepingSchedule := default $defaultHousekeepingSchedule $pvc.backup.housekeepingSchedule }}
{{- $_ := set $backupTracker "enabled" true }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: restic-backup-{{ $pvcName }}
  namespace: {{ $.Release.Namespace }}
data:
  RETRY_LOCK: "15m"
  HOSTNAME: "{{ $.Release.Namespace }}-{{ $pvcName }}"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: restic-backup-{{ $pvcName }}
  namespace: {{ $.Release.Namespace }}
spec:
  schedule: "{{ default $defaultSchedule $pvc.backup.schedule }}"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 172800 # 2 days
      backoffLimit: 4
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: restic
              image: {{ printf "%s:%s" $.Values.restic.image.repository $.Values.restic.image.tag }}
              envFrom:
                - secretRef:
                    name: restic-backup
                  prefix: "RESTIC_"
                - configMapRef:
                    name: restic-backup-{{ $pvcName }}
                  prefix: "RESTIC_"
              volumeMounts:
                - name: data
                  mountPath: /data
                - name: restic-cache
                  mountPath: /cache/restic
              command:
                - /bin/sh
                - -c
                - |
                  set -euo pipefail

                  cd /data

                  export RESTIC_PASSWORD=${RESTIC_password}
                  export RESTIC_REPOSITORY=${RESTIC_repository}

                  export RESTIC_CACHE_DIR=/cache/restic

                  # Setup SSH key and known_hosts for SFTP/rsync.net if private key is provided
                  if [ -n "${RESTIC_ssh_private_key:-}" ]; then
                    echo "[ssh] SSH private key found, preparing connection wrapper..."
                    SSH_DIR="/cache/restic/.ssh"
                    mkdir -p "$SSH_DIR"
                    chmod 700 "$SSH_DIR"

                    # Write the private key
                    echo "$RESTIC_ssh_private_key" > "$SSH_DIR/id_rsa"
                    chmod 600 "$SSH_DIR/id_rsa"

                    # Write known_hosts if fingerprint/known_hosts is provided
                    if [ -n "${RESTIC_ssh_known_hosts:-}" ]; then
                      echo "$RESTIC_ssh_known_hosts" > "$SSH_DIR/known_hosts"
                      chmod 600 "$SSH_DIR/known_hosts"
                    elif [ -n "${RESTIC_ssh_host_fingerprint:-}" ]; then
                      echo "$RESTIC_ssh_host_fingerprint" > "$SSH_DIR/known_hosts"
                      chmod 600 "$SSH_DIR/known_hosts"
                    else
                      touch "$SSH_DIR/known_hosts"
                      chmod 600 "$SSH_DIR/known_hosts"
                    fi

                    # Create a wrapper script to run ssh with correct options
                    printf '%s\n' \
                      '#!/bin/sh' \
                      'set -eu' \
                      "RESTIC_REPOSITORY='${RESTIC_repository:-}'" \
                      'TEMP="${RESTIC_REPOSITORY#sftp:}"' \
                      'TEMP="${TEMP#//}"' \
                      'SSH_HOST="${TEMP%%:*}"' \
                      'SSH_HOST="${SSH_HOST%%/*}"' \
                      'SSH_PORT_ARG=""' \
                      'PORT_AND_PATH="${TEMP#*:}"' \
                      'if [ "$PORT_AND_PATH" != "$TEMP" ]; then' \
                      '  PORT="${PORT_AND_PATH%%/*}"' \
                      '  case "$PORT" in' \
                      '    [0-9]*)' \
                      '      SSH_PORT_ARG="-p $PORT"' \
                      '      ;;' \
                      '  esac' \
                      'fi' \
                      'exec ssh -i /cache/restic/.ssh/id_rsa -o UserKnownHostsFile=/cache/restic/.ssh/known_hosts -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes ${SSH_PORT_ARG} "$SSH_HOST" -s sftp' \
                      > "$SSH_DIR/ssh-wrapper.sh"
                    chmod 700 "$SSH_DIR/ssh-wrapper.sh"

                    export RESTIC_SFTP_ARGS="-o sftp.command=/cache/restic/.ssh/ssh-wrapper.sh"
                  else
                    export RESTIC_SFTP_ARGS=""
                  fi

                  # Define restic wrapper to automatically inject SSH options if configured
                  restic() {
                    if [ -n "${RESTIC_SFTP_ARGS:-}" ]; then
                      command restic ${RESTIC_SFTP_ARGS} "$@"
                    else
                      command restic "$@"
                    fi
                  }

                  # Automatically initialize the repository if it does not exist
                  if ! restic snapshots > /dev/null 2>&1; then
                    echo "[backup] Repository not found, initializing repository..."
                    restic init
                  else
                    echo "[backup] Repository already initialized, skipping initialization."
                  fi

                  echo "[backup] Running backup of /data ..."
                  restic backup . \
                    --skip-if-unchanged \
                    --exclude-if-present NO_BACKUP.TAG \
                    --retry-lock "$RESTIC_RETRY_LOCK" \
                    --host "$RESTIC_HOSTNAME" \
                    --tag="pvc={{ $pvcName }}" \
                    --tag="namespace={{ $.Release.Namespace }}"

                  echo "[backup] Done."

          volumes:
            - name: data
              persistentVolumeClaim:
                claimName: {{ $pvcName }}
            - name: restic-cache
              persistentVolumeClaim:
                claimName: {{ printf "%s-restic-cache" $pvcName }}

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: restic-backup-{{ $pvcName }}-housekeeping
  namespace: {{ $.Release.Namespace }}
spec:
  schedule: "{{ $housekeepingSchedule }}"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 172800 # 2 days
      backoffLimit: 4
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: restic
              image: {{ printf "%s:%s" $.Values.restic.image.repository $.Values.restic.image.tag }}
              envFrom:
                - secretRef:
                    name: restic-backup
                  prefix: "RESTIC_"
                - configMapRef:
                    name: restic-backup-{{ $pvcName }}
                  prefix: "RESTIC_"
              volumeMounts:
                - name: restic-cache
                  mountPath: /cache/restic
              command:
                - /bin/sh
                - -c
                - |
                  set -euo pipefail

                  export RESTIC_PASSWORD=${RESTIC_password}
                  export RESTIC_REPOSITORY=${RESTIC_repository}

                  export RESTIC_CACHE_DIR=/cache/restic

                  # Setup SSH key and known_hosts for SFTP/rsync.net if private key is provided
                  if [ -n "${RESTIC_ssh_private_key:-}" ]; then
                    echo "[ssh] SSH private key found, preparing connection wrapper..."
                    SSH_DIR="/cache/restic/.ssh"
                    mkdir -p "$SSH_DIR"
                    chmod 700 "$SSH_DIR"

                    # Write the private key
                    echo "$RESTIC_ssh_private_key" > "$SSH_DIR/id_rsa"
                    chmod 600 "$SSH_DIR/id_rsa"

                    # Write known_hosts if fingerprint/known_hosts is provided
                    if [ -n "${RESTIC_ssh_known_hosts:-}" ]; then
                      echo "$RESTIC_ssh_known_hosts" > "$SSH_DIR/known_hosts"
                      chmod 600 "$SSH_DIR/known_hosts"
                    elif [ -n "${RESTIC_ssh_host_fingerprint:-}" ]; then
                      echo "$RESTIC_ssh_host_fingerprint" > "$SSH_DIR/known_hosts"
                      chmod 600 "$SSH_DIR/known_hosts"
                    else
                      touch "$SSH_DIR/known_hosts"
                      chmod 600 "$SSH_DIR/known_hosts"
                    fi

                    # Create a wrapper script to run ssh with correct options
                    printf '%s\n' \
                      '#!/bin/sh' \
                      'set -eu' \
                      "RESTIC_REPOSITORY='${RESTIC_repository:-}'" \
                      'TEMP="${RESTIC_REPOSITORY#sftp:}"' \
                      'TEMP="${TEMP#//}"' \
                      'SSH_HOST="${TEMP%%:*}"' \
                      'SSH_HOST="${SSH_HOST%%/*}"' \
                      'SSH_PORT_ARG=""' \
                      'PORT_AND_PATH="${TEMP#*:}"' \
                      'if [ "$PORT_AND_PATH" != "$TEMP" ]; then' \
                      '  PORT="${PORT_AND_PATH%%/*}"' \
                      '  case "$PORT" in' \
                      '    [0-9]*)' \
                      '      SSH_PORT_ARG="-p $PORT"' \
                      '      ;;' \
                      '  esac' \
                      'fi' \
                      'exec ssh -i /cache/restic/.ssh/id_rsa -o UserKnownHostsFile=/cache/restic/.ssh/known_hosts -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes ${SSH_PORT_ARG} "$SSH_HOST" -s sftp' \
                      > "$SSH_DIR/ssh-wrapper.sh"
                    chmod 700 "$SSH_DIR/ssh-wrapper.sh"

                    export RESTIC_SFTP_ARGS="-o sftp.command=/cache/restic/.ssh/ssh-wrapper.sh"
                  else
                    export RESTIC_SFTP_ARGS=""
                  fi

                  # Define restic wrapper to automatically inject SSH options if configured
                  restic() {
                    if [ -n "${RESTIC_SFTP_ARGS:-}" ]; then
                      command restic ${RESTIC_SFTP_ARGS} "$@"
                    else
                      command restic "$@"
                    fi
                  }

                  echo "[restic unlock] Starting stale lock removal operation"
                  restic unlock

                  echo "[retention] Applying retention policy and pruning..."
                  restic forget \
                    --keep-last "{{ $keepLast }}" \
                    --keep-hourly "{{ $keepHourly }}" \
                    --keep-daily "{{ $keepDaily }}" \
                    --keep-weekly "{{ $keepWeekly }}" \
                    --keep-monthly "{{ $keepMonthly }}" \
                    --keep-yearly "{{ $keepYearly }}" \
                    --prune \
                    --retry-lock "$RESTIC_RETRY_LOCK" \
                    --tag="pvc={{ $pvcName }}" \
                    --tag="namespace={{ $.Release.Namespace }}"

                  echo "[check] Verifying repository integrity..."
                  restic check \
                    --retry-lock "$RESTIC_RETRY_LOCK"

                  echo "[housekeeping] Done."

          volumes:
            - name: restic-cache
              persistentVolumeClaim:
                claimName: {{ printf "%s-restic-cache" $pvcName }}

{{- end }}
{{- end }}

{{- if $backupTracker.enabled }}
{{- /* Expose only the External Secrets inputs we need for restic backups */}}
{{- $clusterName := default $.Release.Namespace $.Values.clusterName }}
{{- $subPathSecret := dict "name" "restic-backup" "remoteRefKey" (printf "/k8s-%s/restic-backup" $clusterName) }}
{{- $externalValues := deepCopy .Values }}
{{- $_ := set $externalValues "SubPathSecrets" (list $subPathSecret) }}
{{- include "external-secret-manager.subpath" (dict "Values" $externalValues "Release" .Release "Chart" .Chart "Capabilities" .Capabilities) -}}
{{- end }}

{{- range $pvcName, $pvc := .Values.persistentVolumeClaims }}
{{- if and $pvc.backup $pvc.backup.enabled }}
{{ include "library-app-chart.pvc.claim" (dict "name" (printf "%s-restic-cache" $pvcName) "pvc" $pvc "root" $) }}
{{- end }}
{{- end }}
