# library-app-chart

This chart will house shared Go template helpers that can be reused by sibling charts. It currently ships without templates so that new helpers can be developed locally alongside `app-chart` and `meta-app-chart` using the same version number.
