# meta-app-chart

This chart will eventually compose a collection of application bundles while sharing helpers from `library-app-chart`. It currently ships without any templates so that we can iterate on structure and dependencies inside this repository.
